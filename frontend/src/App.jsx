import React from 'react'
import './App.css'
import HomePage from './pages/HomePage.jsx'
/* deleted the router FOR NOW (we can add this after we have the rest of the pages figured out) */
const App = () => {
  return <HomePage />
}

export default App
